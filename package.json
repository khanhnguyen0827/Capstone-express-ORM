{
  "name": "image-gallery-api",
  "version": "1.0.0",
  "description": "Image Gallery API with Node.js, Express, and Prisma",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "db:migrate": "npx prisma migrate dev",
    "test": "echo \"Error: no test specified\" && exit 1",
     "start": "node --import=extensionless/register server.js",
    "dev": "nodemon --import=extensionless/register  server.js",
    "dev2": "nodemon --import=extensionless/register --env-file .env  server.js"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "swagger-ui-express": "^5.0.0",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prisma": "^5.7.0"
  }
}
